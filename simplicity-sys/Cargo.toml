[package]
name = "simplicity_sys"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[build-dependencies]
cc = "1.0"

[dependencies]
libc = "0.2"
bitcoin_hashes = "0.11"

[features]
test-utils = []